# build output
dist/

# generated types
.astro/

# dependencies
node_modules/

# environment variables
.env
.env.production

# macOS
.DS_Store

# jetbrains / vscode local settings
.idea/
.vscode/

# regenerated on every deploy by scripts/fetch-ocean-assets.py
# (a snapshot is committed so local dev and offline builds work)
# The map's data lives in its own repository now — see MAP_DATA in
# src/config.ts. The pipelines still write here when run locally, so that a
# developer can point dataBase at their own machine; none of it is committed.
public/map/
!public/map/.gitkeep

